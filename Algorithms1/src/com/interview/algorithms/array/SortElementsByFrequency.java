package com.interview.algorithms.array;

/**
 * Print the elements of an array in the decreasing frequency if 2 numbers have
 * same frequency then print the one which came 1st E.g. 2 5 2 8 5 6 8 8 output:
 * 8 8 8 2 2 5 5 6.
 * 
 * @author ajitkoti
 *
 */
public class SortElementsByFrequency {

	/**
	 * Insert elements in BST one by one and if an element is already present
	 * then increment the count of the node. Node of the Binary Search Tree
	 * (used in this approach) will be as follows.
	 */
	void insertIntoBST(int element) {
	}

}
